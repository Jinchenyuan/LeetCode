/*
 * @lc app=leetcode id=916 lang=cpp
 *
 * [916] Word Subsets
 */

// @lc code=start
#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    vector<string> wordSubsets(vector<string>& A, vector<string>& B) {
        
    }

    string wordCotainChar(string str, char c) {
        int len = str.length();
        for (int i = 0; i < len; i++)
        {
            if ()
        }
        
    }
};
// @lc code=end

